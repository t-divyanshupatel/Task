diff --git a/service/app/main_base.py b/service/app/main.py
--- a/service/app/main_base.py
+++ b/service/app/main.py
@@ -1,22 +1,18 @@
-"""Uninstrumented baseline — used only to generate instrumentation-diff.patch proof."""
 from __future__ import annotations
 
-import logging
-import sys
 from typing import Literal
 
 from fastapi import FastAPI, HTTPException
 from pydantic import BaseModel, Field
 
-logging.basicConfig(
-    level=logging.INFO,
-    format="%(asctime)s service=d6-order-api %(message)s",
-    stream=sys.stdout,
+from app.observability import (
+    ObservabilityMiddleware,
+    metrics_asgi_app,
+    record_order_created,
+    setup_logging,
 )
-logger = logging.getLogger("d6-order-api")
 
 OrderStatus = Literal["pending", "confirmed", "shipped"]
 
 _orders: list[dict] = []
 _next_id = 1
+logger = setup_logging()
 
 
 class OrderCreate(BaseModel):
@@ -32,7 +28,9 @@ class OrderResponse(BaseModel):
     status: OrderStatus
 
 
-app = FastAPI(title="D6 Order API (baseline)", version="1.0.0")
+app = FastAPI(title="D6 Order API", version="1.0.0")
+app.add_middleware(ObservabilityMiddleware, logger=logger)
+app.mount("/metrics", metrics_asgi_app())
 
 
 @app.get("/health")
@@ -55,12 +53,7 @@ def create_order(body: OrderCreate) -> OrderResponse:
         "status": "pending",
     }
     _orders.append(order)
-    logger.info(
-        "event=order_created order_id=%s sku=%s quantity=%s",
-        order["id"],
-        order["sku"],
-        order["quantity"],
-    )
+    record_order_created(logger, order["id"], order["sku"], order["quantity"])
     _next_id += 1
     return OrderResponse(**order)
 

=== New file: service/app/observability.py ===
# Structured JSON logging + Prometheus metrics middleware.
# Exposes counters: http_requests_total, http_request_duration_seconds, orders_created_total
# See service/app/observability.py for full implementation.

=== requirements.txt ===
+prometheus-client==0.21.1
